export interface Image {
    id: number;
    description?: string;
    path: string;
    title?: string;
}

export interface ImageDTO {
    id?: string;
    description?: string;
    path: string;
    title?: string;
}
