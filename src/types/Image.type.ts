export interface Image {
    id: number;
    description?: string;
    path: string;
    title?: string;
}

export interface ImageDTO {
    description?: string;
    path: string;
    title?: string;
}
